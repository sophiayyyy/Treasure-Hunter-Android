<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/dark_blue</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textAllCaps">false</item>>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="TopButton" parent="ButtonStyle">
        <item name="android:layout_width">200dp</item>
        <item name="android:background">@drawable/btn_top</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="BottomButton" parent="ButtonStyle">
        <item name="android:layout_width">200dp</item>
        <item name="android:background">@drawable/btn_bottom</item>
        <item name="android:textColor">@color/dark_blue</item>
    </style>

    <style name="ToggleButton" parent="ButtonStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="BottomButton2" parent="ButtonStyle">
        <item name="android:layout_width">150dp</item>
        <item name="android:background">@drawable/btn_bottom2</item>
        <item name="android:textColor">@color/dark_blue</item>
    </style>

    <style name="tag">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
